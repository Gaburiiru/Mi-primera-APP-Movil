<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/mandoo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tp_parte_2"
        tools:targetApi="31">
        <activity
            android:name=".Login.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--activity crearUser-->
        <activity android:name=".Login.Crear_user.CrearUsuarioActivity"/>
        <!--activity panatlla principal-->
        <activity android:name=".PantallaPrincipalActivity"/>
        <!--activity biblioteca-->
        <activity android:name=".biblioteca.BibliotecaActivity"/>
        <!--activity compras-->
        <activity android:name=".ComprasActivity"/>
        <!--activity recargar-->
        <activity android:name=".RecargarActivity"/>
        <!--activity carrito-->
        <activity android:name=".biblioteca.CarritoActivity"/>
    </application>

</manifest>